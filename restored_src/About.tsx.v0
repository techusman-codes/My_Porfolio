"use client";

import { motion } from "framer-motion";
import styles from "./About.module.css";
import { FaDownload } from "react-icons/fa";

export default function About() {
  return (
    <section id="about" className={`section ${styles.about}`}>
      <div className="container">
        <motion.div 
          className={styles.sectionHeader}
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
        >
          <span className="subtitle">Who I Am</span>
          <h2 className="title">About Me</h2>
        </motion.div>

        <div className={styles.aboutContent}>
          <motion.div 
            className={styles.aboutText}
            initial={{ opacity: 0, x: -50 }}
            whileInView={{ opacity: 1, x: 0 }}
            viewport={{ once: true }}
            transition={{ delay: 0.2 }}
          >
            <p>
              I am a dedicated <strong>Mobile & Backend Engineer</strong> passionate about building
              high-quality, cross-platform applications. I leverage <strong>Flutter</strong> for seamless
              UI/UX and design scalable backend architectures with <strong>Python/FastAPI</strong>.
            </p>
            <p>
              My work focuses on crafting immersive user experiences, clean UI systems, and efficient data
              flows. I’m also deeply invested in the future of global payments, integrating <strong>Bitcoin
              and Lightning Network</strong> infrastructure into modern applications to enable secure,
              borderless transactions.
            </p>

            <div className={styles.infoGrid}>
              <div className={styles.infoItem}>
                <span className={styles.label}>From:</span>
                <span className={styles.value}>Kaduna, Nigeria</span>
              </div>
              <div className={styles.infoItem}>
                <span className={styles.label}>Email:</span>
                <span className={styles.value}>ugarba202@gmail.com</span>
              </div>
              <div className={styles.infoItem}>
                <span className={styles.label}>Phone:</span>
                <span className={styles.value}>+234 814 007 9634</span>
              </div>
              <div className={styles.infoItem}>
                <span className={styles.label}>Freelance:</span>
                <span className={`${styles.value} ${styles.available}`}>Available</span>
              </div>
            </div>

            <a href="https://docs.google.com/document/d/1-RnfbqxTk7atDfUjgUuL2G9y5NLOdK4LzrLRuMJM054/edit?tab=t.0" target="_blank" rel="noopener noreferrer" className="btn btn-primary">
              Download CV <FaDownload />
            </a>
          </motion.div>

          <motion.div 
            className={styles.aboutStats}
            initial={{ opacity: 0, x: 50 }}
            whileInView={{ opacity: 1, x: 0 }}
            viewport={{ once: true }}
            transition={{ delay: 0.4 }}
          >
            <div className={`glass ${styles.statCard}`}>
              <h3 className={styles.statNumber}>2+</h3>
              <p className={styles.statLabel}>Years Experience</p>
            </div>
            <div className={`glass ${styles.statCard}`}>
              <h3 className={styles.statNumber}>10+</h3>
              <p className={styles.statLabel}>Projects Completed</p>
            </div>
            <div className={`glass ${styles.statCard}`}>
              <h3 className={styles.statNumber}>100%</h3>
              <p className={styles.statLabel}>Client Satisfaction</p>
            </div>
          </motion.div>
        </div>
      </div>
    </section>
  );
}
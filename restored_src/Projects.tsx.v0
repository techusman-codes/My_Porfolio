"use client";

import { motion } from "framer-motion";
import Image from "next/image";
import styles from "./Projects.module.css";
import { FaGithub, FaExternalLinkAlt } from "react-icons/fa";

export default function Projects() {
  const projects = [
    {
      title: "E-Commerce Platform",
      description: "Grabby is a full-stack mobile e-commerce application designed to revolutionize online shopping in Nigeria. Built with Flutter, Grabby addresses critical gaps in existing e-commerce platforms.",
      tags: ["Flutter", "Dart", "Firebase", "Paystack"],
      image: "/images/project1.png",
      github: "https://github.com/Ugarba202/Grabby",
      live: "https://github.com/Ugarba202/Grabby"
    },
    {
      title: "Lightning Pay Wallet",
      description: "LightningPay is a learning-focused Bitcoin & Lightning payment app built with Flutter, designed to demonstrate how modern fintech systems work internally.",
      tags: ["Flutter", "LDK", "Bitcoin"],
      image: "/images/project2.png",
      github: "https://github.com/Ugarba202/LightningPay",
      live: "https://lightningpay.globeapp.dev/"
    },
    {
      title: "Logistics App",
      description: "SamLogistics is a modern, responsive logistics app UI built with Flutter. This repository contains the full UI for a logistics product with 14 screens designed from scratch.",
      tags: ["Flutter", "FastAPI", "PostgreSQL"],
      image: "/images/project3.png",
      github: "https://github.com/Ugarba202/Logistack",
      live: "https://github.com/Ugarba202/Logistack"
    },
    {
      title: "AI Habit Tracker App",
      description: "The app helps users build and sustain positive habits through reminders, progress tracking, and smart analytics.",
      tags: ["Flutter", "Firebase", "UI/UX"],
      image: "/images/project4.png",
      github: "https://ai-habit-tracker-app-whucxsy-usman-umar-garba.globeapp.dev/",
      live: "https://ai-habit-tracker-app-whucxsy-usman-umar-garba.globeapp.dev/"
    },
    {
      title: "Linkme_up",
      description: "Interactive analytical dashboard for personal finance tracking with visual data representation.",
      tags: ["Flutter Web", "Chart.js", "Analytics"],
      image: "/images/project1.png",
      github: "https://github.com/Ugarba202",
      live: "#"
    },
    {
      title: "Real estate mobile App",
      description: "A modern and responsive Real Estate App UI built with Flutter. This project focuses on clean UI design, reusable widgets, and responsive layouts.",
      tags: ["Flutter", "UI/UX", "Dart"],
      image: "/images/project2.png",
      github: "https://github.com/Ugarba202/real_estate_UI",
      live: "https://real-estate-ui.globeapp.dev/"
    }
  ];

  return (
    <section id="portfolio" className={`section ${styles.projects}`}>
      <div className="container">
        <motion.div 
          className={styles.sectionHeader}
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
        >
          <span className="subtitle">My Work</span>
          <h2 className="title">Featured Projects</h2>
        </motion.div>

        <div className={styles.projectsGrid}>
          {projects.map((project, index) => (
            <motion.div 
              key={index}
              className={`glass ${styles.projectCard}`}
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ delay: index * 0.1 }}
            >
              <div className={styles.projectImg}>
                <Image 
                  src={project.image} 
                  alt={project.title} 
                  fill
                  style={{ objectFit: 'cover' }}
                />
                <div className={styles.overlay}>
                  <div className={styles.links}>
                    <a href={project.github} target="_blank" rel="noopener noreferrer"><FaGithub /></a>
                    <a href={project.live} target="_blank" rel="noopener noreferrer"><FaExternalLinkAlt /></a>
                  </div>
                </div>
              </div>
              <div className={styles.projectContent}>
                <h3>{project.title}</h3>
                <p>{project.description}</p>
                <div className={styles.tags}>
                  {project.tags.map(tag => (
                    <span key={tag}>{tag}</span>
                  ))}
                </div>
              </div>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  );
}